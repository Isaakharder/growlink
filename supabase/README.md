# Supabase

This folder is reserved for database migrations and Supabase project configuration.

- Place SQL migrations in `supabase/migrations/`
- Keep migrations incremental and versioned
- Avoid business-specific schema complexity in this shell phase
